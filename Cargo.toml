[package]
name = "srvchk"
version = "0.0.1"
edition = "2021"

[dependencies]
clap-verbosity-flag = "2.2.0"

[dependencies.clap]
version = "4.5.8"
default-features = false
features = ["std", "help", "derive"]

[profile.release]
codegen-units = 1
lto = true
panic = "abort"
strip = true
